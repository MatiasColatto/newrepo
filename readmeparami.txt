Esta cosa lo que hace en resumen es Levanta el backend, espera que esté listo, 
ejecuta pruebas de carga con k6 y luego genera un reporte automático con Python.

Tuve que hacer un archivo python para generar el reporte con los datos json que el k6 deja con un comando

run_performance_test.bat